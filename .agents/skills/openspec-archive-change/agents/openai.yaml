interface:
  display_name: "OpenSpec Archive Change"
  short_description: "Validate and archive a completed OpenSpec change"
  default_prompt: "Use $openspec-archive-change to validate and archive a completed OpenSpec change."
