//! Experiment: sync documents over `iroh-docs`, bypassing willow.
//!
//! Scenario tests driving the public API of `mee-sync-iroh-docs` (the
//! data-layer adapter over our capability-gated iroh-docs variant) —
//! just enough to validate that the stack can host the
//! document-replication primitives we need before committing further.
