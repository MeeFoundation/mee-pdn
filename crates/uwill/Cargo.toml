[package]
name = "uwill"
version = "0.1.0"
edition = "2021"
description = "UWill capability tokens: delegation token format and (future) chain validation"

[dependencies]
mee-types = { path = "../mee-types" }
serde = { version = "1", features = ["derive"] }

[lints]
workspace = true
