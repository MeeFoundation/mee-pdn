[package]
name = "mee-identity-api"
version = "0.1.0"
edition = "2021"

[dependencies]
mee-types = { path = "../mee-types" }
serde = { version = "1", features = ["derive"] }
thiserror = "2.0.16"

[lints]
workspace = true
