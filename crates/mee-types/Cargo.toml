[package]
name = "mee-types"
version = "0.1.0"
edition = "2021"
description = "Shared domain types"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
serde_json = "1"

[lints]
workspace = true
