[package]
name = "mee-pdn-layer"
version = "0.1.0"
edition = "2021"
description = "Draft: PDN-layer"

[dependencies]
mee-types = { path = "../mee-types" }
mee-sync-api = { path = "../mee-sync-api" }
serde = { version = "1", features = ["derive"] }

[lints]
workspace = true
